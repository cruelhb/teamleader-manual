# 내용 추가·수정 안내서

이 사이트를 혼자 관리할 때 보는 문서입니다.
**개발 지식 없이도 이 문서만 보면 내용을 추가할 수 있게** 썼습니다.

---

## 1. 먼저 알아둘 것

- 글은 전부 `src/content/` 폴더 안에 **텍스트 파일(.md)** 로 들어 있습니다.
- 파일 하나 = 사이트 페이지 하나입니다.
- **파일 이름이 곧 주소**가 됩니다.
  예) `src/content/welfare/연차-사용-기준.md` → `사이트주소/welfare/연차-사용-기준/`
- 파일 이름에 **띄어쓰기 대신 하이픈(-)** 을 쓰세요.

### 폴더별 용도

| 폴더 | 메뉴 | 넣을 내용 |
| --- | --- | --- |
| `src/content/notice/` | 📢 공지사항 | 전 인원 공지 |
| `src/content/guide/` | 📘 이용 방법 | 전산·시스템 사용법 (스크린샷 위주) |
| `src/content/welfare/` | 🎁 복지·인사 | 연차, 경조사, 교육비 등 규정 |
| `src/content/faq/` | ❓ FAQ | 자주 묻는 질문 |
| `src/content/form/` | 📄 서식·양식 | 다운로드용 서식 안내 |

---

## 2. 새 글 추가하기

해당 폴더에 `.md` 파일을 하나 만들고 아래 형식으로 씁니다.

```markdown
---
title: 연차 사용 기준
summary: 연차 발생 기준과 신청 방법을 안내합니다.
tags: [연차, 휴가, 잔여연차]
updated: 2026-07-27
source: 취업규칙 제32조
order: 10
---

## 연차 발생 기준

여기에 본문을 씁니다.
```

### 맨 위 `---` 사이 항목 설명

| 항목 | 필수 | 설명 |
| --- | --- | --- |
| `title` | ✅ | 제목. **팀원이 실제로 검색할 단어**를 쓰세요 |
| `summary` | ✅ | 한 줄 요약. 검색 결과와 **카톡 미리보기에 그대로 뜹니다** |
| `updated` | ✅ | 최종 수정일 (`2026-07-27` 형식). 내용을 고칠 때마다 **꼭 같이 고치세요** |
| `tags` | | 검색용 추가 단어. 제목에 없는 다른 표현을 넣으면 좋습니다 |
| `source` | | 근거 규정 (예: `취업규칙 제32조`) |
| `order` | | 목록 정렬 순서. 숫자가 작을수록 위 |
| `pinned` | | `true` 로 하면 목록 맨 위에 📌 중요 표시로 고정 |
| `file` | | 서식 파일 경로 (예: `/forms/휴가원.hwp`) |

> ⚠️ `title`·`summary`·`updated` 중 하나라도 빠지면 **배포가 실패합니다.**
> 일부러 그렇게 만들었습니다 — 날짜 없는 규정 안내는 아무도 믿지 않기 때문입니다.

### tags 를 잘 쓰는 게 검색 품질을 좌우합니다

팀원들은 규정집 용어가 아니라 **자기 말**로 검색합니다.
제목이 "경조휴가 일수"라면 tags 에는 이런 걸 넣으세요.

```yaml
tags: [경조휴가, 상갓집, 부고, 조사, 결혼, 축의금]
```

---

## 3. 본문 쓰는 법 (마크다운)

```markdown
## 큰 제목
### 작은 제목

그냥 쓰면 문단이 됩니다.

- 목록 항목
- 목록 항목

1. 번호 목록
2. 번호 목록

**굵게** 강조합니다.

> 강조하고 싶은 안내문은 이렇게 씁니다.

| 구분 | 일수 |
| --- | --- |
| 본인 결혼 | 5일 |
| 배우자 출산 | 10일 |

[다른 문서로 링크](/welfare/연차-사용-기준/)
```

표는 좁은 폰 화면에서 **자동으로 가로 스크롤**되니 열이 많아도 괜찮습니다.

---

## 4. 스크린샷 넣기

1. 이미지 파일을 `public/img/` 안에 넣습니다.
   - 이용 방법 캡처면 `public/img/guide/` 처럼 폴더를 나누면 관리가 쉽습니다.
2. 본문에 이렇게 씁니다.

```markdown
![인트라넷 로그인 화면](/img/guide/비밀번호변경-01.png)
```

- 경로는 `public` 을 빼고 `/img/...` 부터 씁니다.
- 대괄호 안 설명은 이미지가 안 뜰 때 대신 보이는 글자입니다.
- 사이트에서 **이미지를 탭하면 전체 화면으로 확대**됩니다. 캡처 속 작은 글씨도 읽을 수 있습니다.

---

## 5. 서식 파일 올리기

1. 파일을 `public/forms/` 에 넣습니다. (예: `public/forms/휴가원.hwp`)
2. `src/content/form/휴가원.md` 의 맨 위에 경로를 추가합니다.

```yaml
file: /forms/휴가원.hwp
```

그러면 페이지 상단에 **내려받기 버튼**이 생깁니다.

---

## 6. 홈 화면 손보기

### 자주 찾는 항목 (타일 8개)

`src/data/shortcuts.json` 을 고칩니다.

```json
{ "label": "연차 사용 기준", "url": "/welfare/연차-사용-기준/", "emoji": "🌴" }
```

### 사이트 이름·제보 링크

`src/data/site.json` 을 고칩니다.

```json
{
  "name": "우리팀 안내센터",
  "tagline": "사내 규정 · 공지 · 이용 방법을 한 곳에서",
  "reportUrl": ""
}
```

- `reportUrl` 에 구글폼이나 카카오 오픈채팅 주소를 넣으면
  **모든 문서 하단에 제보 버튼**이 생깁니다. 비워두면 버튼이 안 보입니다.
- 사이트 이름을 바꿨다면 카톡 미리보기 이미지도 다시 만드세요.

```bash
powershell -ExecutionPolicy Bypass -File scripts\make-og-image.ps1
```

---

## 7. 내 PC에서 확인하기

```bash
npm run dev
```

브라우저에서 `http://localhost:4321` 을 엽니다.
파일을 저장하면 **화면이 자동으로 바뀝니다.** 확인이 끝나면 터미널에서 `Ctrl + C`.

배포 전에 문제가 없는지 확인하려면:

```bash
npm run build
```

에러 없이 끝나면 배포해도 됩니다.

---

## 8. 배포하기

```bash
git add .
git commit -m "연차 규정 업데이트"
git push
```

푸시하면 Cloudflare Pages가 **1~2분 안에 자동으로** 사이트를 갱신합니다.

커밋 메시지는 나중에 "이 기준이 언제 바뀌었지?"를 추적하는 **규정 변경 이력**이 되니
`수정` 대신 `경조휴가 일수 5일→7일 변경`처럼 구체적으로 쓰는 편이 좋습니다.

---

## 9. 카카오톡 미리보기가 옛날 내용으로 뜰 때

카카오톡은 링크 미리보기를 **한 번 저장해두고 계속 재사용**합니다.
제목이나 요약을 고쳤는데 옛날 걸로 뜬다면 캐시를 지워야 합니다.

1. <https://developers.kakao.com/tool/clear/og> 접속
2. 해당 페이지 주소를 넣고 초기화

---

## 10. 배포가 실패했을 때

거의 대부분 아래 셋 중 하나입니다.

| 증상 | 원인 | 해결 |
| --- | --- | --- |
| `title is required` 류의 에러 | 맨 위 `---` 안에 필수 항목이 빠짐 | `title`·`summary`·`updated` 를 채웁니다 |
| `Invalid date` | 날짜 형식이 틀림 | `2026-07-27` 형식으로 씁니다 |
| 이미지가 안 보임 | 경로에 `public` 이 들어감 | `/img/...` 로 시작하게 고칩니다 |

---

## 11. Claude Code 에게 시키기

파일을 직접 고치는 게 번거로우면 이렇게 말해도 됩니다.

> 경조휴가 일수 문서에서 배우자 출산을 10일에서 20일로 바꾸고 수정일도 오늘로 갱신해줘

> 캡처 3장 `public/img/guide/` 에 넣어뒀어. 이걸로 전자결재 상신 방법 문서 만들어줘

> 팀원이 "재택근무 신청 어떻게 해요?"라고 자주 물어봐. FAQ 문서 하나 만들어줘
