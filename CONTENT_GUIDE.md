# 내용 추가·수정 안내서

이 사이트를 혼자 관리할 때 보는 문서입니다.

글을 올리는 방법은 **두 가지**입니다.

| | 관리자 화면 | 파일 직접 편집 |
| --- | --- | --- |
| 어디서 | 브라우저 `/admin` 주소 | 내 PC의 파일 |
| 난이도 | 쉬움 (입력칸 채우기) | 조금 익숙해져야 함 |
| 이미지 | 드래그해서 업로드 | 폴더에 복사 |
| 배포 | 저장하면 자동 | `git push` 직접 |

**평소에는 관리자 화면(2장)** 을 쓰시고, 표가 많은 복잡한 문서는 파일 편집(3장)이 편할 수 있습니다.

---

## 1. 먼저 알아둘 것

- 글은 전부 `src/content/` 폴더 안에 **텍스트 파일(.md)** 로 저장됩니다.
- 파일 하나 = 사이트 페이지 하나입니다.
- **제목이 곧 주소**가 됩니다.
  예) 제목이 `연차 사용 기준` → `사이트주소/welfare/연차-사용-기준/`

### 메뉴별 용도

| 메뉴 | 폴더 | 넣을 내용 |
| --- | --- | --- |
| 공지사항 | `src/content/notice/` | 전 인원 공지 |
| 업무 매뉴얼 | `src/content/guide/` | 전산·시스템 사용법 (스크린샷 위주) |
| 복지·인사 규정 | `src/content/welfare/` | 연차, 경조사, 교육비 등 규정 |

---

## 2. 관리자 화면으로 글 등록하기

주소 끝에 `/admin` 을 붙이면 글쓰기 화면이 열립니다.

### 방법 A — 내 PC에서 쓰기 (계정 불필요)

> ⚠️ **크롬 또는 엣지 브라우저**에서만 됩니다. (파이어폭스·사파리 미지원)

1. 터미널에서 `npm run dev` 실행
2. 브라우저에서 `http://localhost:4321/admin/` 접속
3. **`Work with Local Repository`** 버튼 클릭
4. 폴더 선택 창이 뜨면 **`teamleader-manual` 폴더**를 선택하고 권한 허용
5. 글을 쓰고 저장하면 내 PC의 파일이 바로 바뀝니다
6. 다 됐으면 터미널에서 `git add . && git commit -m "내용" && git push`

### 방법 B — GitHub 연결해서 쓰기 (폰에서도 가능)

<https://teamleader-manual.pages.dev/admin/> 로 접속합니다.
**저장하면 자동으로 커밋되고 1~2분 뒤 사이트에 반영**됩니다.

> ⚠️ **`Sign In with GitHub` 버튼은 누르지 마세요.**
> 그 버튼은 Netlify에 배포한 경우에만 동작합니다. 이 사이트는 Cloudflare에
> 있어서 `api.netlify.com ... Not Found` 오류가 납니다.
> 아래쪽 **`Sign In Using Access Token`** 을 쓰세요.

**토큰 발급 방법** (처음 한 번만)

1. <https://github.com/settings/personal-access-tokens/new> 접속
2. **Repository access** → `Only select repositories` → `teamleader-manual` 선택
3. **Permissions** → **Repository permissions** → **Contents** 를 `Read and write` 로 변경
4. `Generate token` → 화면에 뜬 토큰 복사 (**이 화면을 벗어나면 다시 볼 수 없습니다**)
5. `/admin/` → `Sign In Using Access Token` → 붙여넣기

토큰은 브라우저에 저장되므로 한 번만 넣으면 됩니다. 폰에서도 같은 방법으로 하면 됩니다.
잘 안 되면 <https://github.com/settings/tokens/new> 에서 `repo` 권한 classic token 으로 시도하세요.

> 🔒 토큰은 비밀번호와 같습니다. 다른 사람에게 알려주지 마세요.
> 만료되거나 유출되면 GitHub 설정에서 삭제하고 새로 발급하면 됩니다.

**참고**: `Sign In with GitHub` 버튼도 쓸 수 있게 만들려면 Cloudflare Workers에
인증 중계 서버를 따로 배포해야 합니다. 토큰 방식으로 불편하지 않다면 굳이 필요 없습니다.

### 화면에서 하는 일

왼쪽에서 메뉴(공지사항/업무 매뉴얼/…)를 고르고 **새 글** 을 누르면 입력칸이 나옵니다.

- **제목** — 팀원이 실제로 검색할 단어 그대로
- **한 줄 요약** — 검색 결과와 카카오톡 미리보기에 그대로 표시됨
- **검색 키워드** — 제목에 없는 다른 표현 (예: 상갓집, 부고, 청첩장)
- **최종 수정일** — 고칠 때마다 오늘 날짜로
- **본문** — 이미지는 여기에 **드래그해서** 올리면 됩니다

> ⚠️ **본문 편집 모드 주의**
> 본문 편집기는 `raw`(원본) 모드가 기본입니다. **그대로 쓰세요.**
> `rich_text` 모드로 바꾸면 [경조사 안내](src/content/welfare/경조사-안내.md) 처럼
> 펼침 목록(`<details>`)이 들어간 문서는 구조가 깨질 수 있습니다.

---

## 3. 파일로 직접 글 추가하기

해당 폴더에 `.md` 파일을 만들고 아래 형식으로 씁니다.

```markdown
---
title: 연차 사용 기준
summary: 연차 발생 기준과 신청 방법을 안내합니다.
tags: [연차, 휴가, 잔여연차]
updated: 2026-07-27
source: 취업규칙 제32조
order: 10
---

## 연차 발생 기준

여기에 본문을 씁니다.
```

파일 이름에는 띄어쓰기 대신 **하이픈(-)** 을 쓰세요.

### 맨 위 `---` 사이 항목

| 항목 | 필수 | 설명 |
| --- | --- | --- |
| `title` | ✅ | 제목 |
| `summary` | ✅ | 한 줄 요약 |
| `updated` | ✅ | 최종 수정일 (`2026-07-27` 형식) |
| `tags` | | 검색용 추가 단어 |
| `source` | | 근거 규정 (예: `취업규칙 제32조`) |
| `order` | | 목록 정렬 순서. 숫자가 작을수록 위 |
| `pinned` | | `true` 면 목록 맨 위에 📌 중요 표시로 고정 |
| `file` | | 서식 파일 경로 (예: `/forms/휴가원.hwp`) |

> ⚠️ `title`·`summary`·`updated` 중 하나라도 빠지면 **배포가 실패합니다.**
> 일부러 그렇게 만들었습니다 — 날짜 없는 규정 안내는 아무도 믿지 않기 때문입니다.

### 검색 키워드(tags)가 검색 품질을 좌우합니다

팀원들은 규정집 용어가 아니라 **자기 말**로 검색합니다.
제목이 "경조사 안내"라면 tags 에는 이런 걸 넣으세요.

```yaml
tags: [경조휴가, 상갓집, 부고, 청첩장, 축의금, 조의금, 돌잔치, 재혼]
```

---

## 4. 본문 쓰는 법 (마크다운)

```markdown
## 큰 제목
### 작은 제목

그냥 쓰면 문단이 됩니다.

- 목록 항목
- 목록 항목

1. 번호 목록
2. 번호 목록

**굵게** 강조합니다.

> 강조하고 싶은 안내문은 이렇게 씁니다.

| 구분 | 일수 |
| --- | --- |
| 본인 결혼 | 5일 |

[다른 문서로 링크](/welfare/경조사-안내/)
```

### 표를 쓸 때 (중요)

**좌우로 밀어야 보이는 표는 만들지 마세요.** 팀원은 대부분 폰으로 봅니다.
표가 옆으로 밀리면 잘린 칸이 있다는 것조차 모르고 지나칩니다.

- 폰과 태블릿(가로 700px 이하)에서는 표가 **자동으로 카드 형태**로 바뀝니다.
- **칸이 3개를 넘거나**, **칸마다 한 문장씩** 들어가면 표를 쓰지 마세요.
  아래 **비교 카드**나 **절차 도식**으로 만드는 편이 훨씬 잘 읽힙니다.
- 표를 쓴다면 칸 수를 줄이고 각 칸을 짧게 유지하세요.

### 두 가지를 비교해서 보여주기

무급휴가와 결근처럼 **두 개를 나란히 놓고 비교**할 때 씁니다.
(예: [무급휴가 사용 기준 및 방법](src/content/welfare/무급휴가-사용-기준-및-방법.md))

```markdown
<ul class="compare">
<li class="is-good"><span class="compare__label">무급휴가</span>회사의 사전 승인을 받은 휴가
<span class="compare__note">인사고과에 불이익이 없습니다.</span></li>
<li class="is-caution"><span class="compare__label">결근</span>임의로 출근하지 않은 상태</li>
</ul>
```

- `is-good` 은 파란 테두리, `is-caution` 은 주황 테두리입니다. 안 붙이면 회색입니다.
- `<span class="compare__note">` 는 카드 안 작은 글씨 설명입니다. 없어도 됩니다.

### 절차를 단계로 보여주기

신청 절차나 결재 순서처럼 **순서가 있는 일**에 씁니다.
(예: [OJT 프로세스 및 진행방법](src/content/guide/OJT-프로세스-및-진행방법.md))

```markdown
<ol class="steps">
<li><span class="step__what">OJT계획서 생성</span><span class="step__who">해당 팀장</span></li>
<li><span class="step__what">OJT계획서 승인</span><span class="step__who">지원부장</span>
<span class="step__note">경력자는 생략할 수 있습니다.</span></li>
</ol>
```

- **번호와 연결선은 자동으로 그려집니다.** 단계를 넣거나 빼도 번호가 알아서 다시 매겨집니다.
- `step__who` 는 그 단계를 처리하는 사람 배지, `step__note` 는 작은 글씨 설명입니다.

> **주의**: 위 두 가지는 `<details>` 와 반대로 **태그 안에 빈 줄을 넣으면 안 됩니다.**
> 빈 줄이 있으면 태그가 글자 그대로 화면에 나옵니다.

### 눌러서 펼치는 목록 만들기

내용이 많은 문서는 **대분류만 먼저 보여주고 누르면 펼쳐지게** 할 수 있습니다.
(예: [경조사 안내](src/content/welfare/경조사-안내.md))

```markdown
<details>
<summary><i class="ico ico--rings"></i> 결혼 <span class="summary-hint">본인 5일 · 70만원</span></summary>

| 대상 | 경조휴가 | 회사지급분 |
| --- | --- | --- |
| 본인 | 5일 | 700,000원 |

- 여기에 설명을 씁니다.

</details>
```

지켜야 할 것 두 가지입니다.

1. `<summary>` 줄 **다음에 반드시 빈 줄**을 넣으세요. 빈 줄이 없으면 표가 표로 안 보이고 글자 그대로 나옵니다.
2. `</details>` **앞에도 빈 줄**을 넣으세요.

`<span class="summary-hint">` 부분은 접힌 상태에서 제목 옆에 작게 보이는 요약입니다. 없어도 됩니다.

펼치는 목록이 2개 이상이면 **"모두 펼치기" 버튼이 자동으로** 생깁니다.

### 본문에 아이콘 넣기

이모지(💍 ⚠️ 같은 그림)는 쓰지 않습니다. 기기마다 그림이 다르게 나오고 색이
어수선해지기 때문입니다. 대신 아래처럼 **아이콘 이름**을 씁니다.

```markdown
<i class="ico ico--alert"></i> **경사는 경조일 2주 전까지 신청해야 합니다.**
```

`ico--` 뒤에 이름만 바꾸면 모양이 바뀝니다. 색은 주변 글자색을 따라가므로
따로 정할 것이 없습니다. 자주 쓸 만한 것들입니다.

| 이름 | 모양 | 어디에 |
| --- | --- | --- |
| `alert` | 삼각형 안 느낌표 | 주의·경고 |
| `info` | 원 안 느낌표 | 꼭 알아둘 것 |
| `minus` | 원 안 뺄셈 기호 | 감액·차감 |
| `won` | 원화 기호 | 금액 |
| `guide` | 점·선 목록 | 절차·방법 |
| `rings` | 겹친 반지 | 결혼 |
| `candle` | 촛불 | 조사 |
| `age` | 겹친 원(나이테) | 고희·산수 등 |
| `star` | 별 | 출산·돌잔치 |
| `calendar` | 달력 | 날짜·기한 |
| `envelope` | 봉투 | 경조사비 |
| `book` | 펼친 책 | 교육 |
| `medical` | 원 안 십자 | 건강검진 |
| `key` | 열쇠 | 비밀번호 |
| `form` | 모서리 접힌 종이 | 서식 |
| `download` | 아래 화살표 | 내려받기 |

없는 이름을 쓰면 아이콘 자리가 빈 칸으로 나옵니다. 새 모양이 필요하면
`src/lib/icons.ts` 에 추가해야 하니 Claude Code 에게 부탁하세요.

---

## 5. 스크린샷 넣기

**관리자 화면**에서는 본문에 이미지를 **드래그**하면 끝입니다.

파일로 직접 넣을 때는:

1. 이미지를 `public/img/` 안에 넣습니다. (예: `public/img/guide/`)
2. 본문에 이렇게 씁니다.

```markdown
![인트라넷 로그인 화면](/img/guide/비밀번호변경-01.png)
```

- 경로는 `public` 을 빼고 `/img/...` 부터 씁니다.
- 사이트에서 **이미지를 탭하면 전체 화면으로 확대**됩니다.

---

## 6. 내려받을 파일 붙이기

문서에 파일(서식 등)을 붙이려면 `public/` 아래에 파일을 넣고
문서 맨 위에 경로를 씁니다.

```yaml
file: /forms/휴가원.hwp
```

그러면 페이지 상단에 **내려받기 버튼**이 생깁니다.

> 지금은 관리자 화면에 이 칸이 없습니다(서식·양식 메뉴를 없애면서 같이 뺐습니다).
> 필요해지면 Claude Code 에게 "파일 첨부 칸 다시 넣어줘"라고 하시면 됩니다.

---

## 7. 홈 화면 손보기

### 자주 찾는 항목 타일

`src/data/shortcuts.json` 을 고칩니다.

```json
{ "label": "경조사 안내", "url": "/welfare/경조사-안내/", "icon": "envelope" }
```

`icon` 에는 **이모지가 아니라 아이콘 이름**을 씁니다. 쓸 수 있는 이름은 아래와 같습니다.

| 이름 | 모양 | 이름 | 모양 |
| --- | --- | --- | --- |
| `envelope` | 봉투 | `calendar` | 달력 |
| `book` | 펼친 책 | `medical` | 원 안의 십자 |
| `form` | 모서리 접힌 종이 | `key` | 열쇠 |
| `notice` | 퍼져 나가는 호 | `guide` | 점·선 목록 |
| `welfare` | 사람 | `faq` | 원 안의 물음표 |
| `search` | 돋보기 | `copy` | 겹친 두 장 |
| `download` | 아래 화살표 | `logo` | 겹친 마름모 |

없는 이름을 넣으면 아이콘이 안 보입니다. 새 모양이 필요하면
`src/lib/icons.ts` 에 추가해야 합니다(이모지는 쓰지 않기로 했습니다 — 기기마다
그림이 달라지고 색이 어수선해집니다).

### 사이트 이름·설명

`src/data/site.json` 을 고칩니다.

- 사이트 이름을 바꿨다면 카톡 미리보기 이미지도 다시 만드세요.

```bash
powershell -ExecutionPolicy Bypass -File scripts\make-og-image.ps1
```

---

## 8. 내 PC에서 확인하기

```bash
npm run dev
```

브라우저에서 `http://localhost:4321` 을 엽니다.
파일을 저장하면 **화면이 자동으로 바뀝니다.** 확인이 끝나면 터미널에서 `Ctrl + C`.

배포 전에 문제가 없는지 확인하려면:

```bash
npm run build
```

---

## 9. 배포하기

관리자 화면을 **GitHub 연결 모드**로 쓰고 있다면 저장할 때 자동으로 배포되므로
아래 작업이 필요 없습니다.

파일을 직접 고쳤다면:

```bash
git add .
git commit -m "경조휴가 일수 5일→7일 변경"
git push
```

푸시하면 Cloudflare Pages가 **1~2분 안에 자동으로** 사이트를 갱신합니다.
커밋 메시지는 나중에 "이 기준이 언제 바뀌었지?"를 추적하는 **규정 변경 이력**이 되니
구체적으로 쓰는 편이 좋습니다.

---

## 10. 카카오톡 미리보기가 옛날 내용으로 뜰 때

카카오톡은 링크 미리보기를 **한 번 저장해두고 계속 재사용**합니다.

1. <https://developers.kakao.com/tool/clear/og> 접속
2. 해당 페이지 주소를 넣고 초기화

---

## 11. 배포가 실패했을 때

거의 대부분 아래 셋 중 하나입니다.

| 증상 | 원인 | 해결 |
| --- | --- | --- |
| `title is required` 류의 에러 | 필수 항목이 빠짐 | `title`·`summary`·`updated` 를 채웁니다 |
| `Invalid date` | 날짜 형식이 틀림 | `2026-07-27` 형식으로 씁니다 |
| 이미지가 안 보임 | 경로에 `public` 이 들어감 | `/img/...` 로 시작하게 고칩니다 |

---

## 12. Claude Code 에게 시키기

관리자 화면을 쓰더라도, 아래처럼 말로 시키는 게 빠를 때가 많습니다.

> 경조사 안내에서 자녀 결혼 축의금을 30만원에서 40만원으로 바꾸고 수정일도 오늘로 갱신해줘

> 캡처 3장 `public/img/guide/` 에 넣어뒀어. 이걸로 전자결재 상신 방법 문서 만들어줘

> 팀원이 "재택근무 신청 어떻게 해요?"라고 자주 물어봐. 업무 매뉴얼에 문서 하나 만들어줘
